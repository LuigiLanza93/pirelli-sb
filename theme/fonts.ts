import { theme as base } from "@chakra-ui/react";

const fonts = {
  fonts: {
    heading: `neue-haas-grotesk-display, ${base.fonts?.heading}, sans-serif`,
    body: `neue-haas-grotesk-text, ${base.fonts?.body}, sans-serif`,
  },
};

export default fonts;
